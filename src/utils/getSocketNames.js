const getUserSocket = (id) => `user:${id}`;

const FRIEND_ADD_SOCKET = 'friend:add';

const FRIEND_DELETE_SOCKET = 'friend:add';

module.exports = {
    getUserSocket,
    FRIEND_ADD_SOCKET,
    FRIEND_DELETE_SOCKET,
};